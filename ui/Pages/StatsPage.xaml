<Page x:Class="CloudRedirect.Pages.StatsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:res="clr-namespace:CloudRedirect.Resources"
      xmlns:conv="clr-namespace:CloudRedirect.Converters"
      ScrollViewer.CanContentScroll="False">

    <Page.Resources>
        <conv:UrlToImageSourceConverter x:Key="UrlToImageSource" />
    </Page.Resources>

    <Grid>
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="28,16">
        <StackPanel>
            <Grid Margin="0,0,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="{res:Loc Nav_Stats}"
                           FontSize="28" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <ui:Button Grid.Column="1"
                           VerticalAlignment="Center"
                           Appearance="Secondary"
                           Icon="{ui:SymbolIcon ArrowSync24}"
                           Content="{res:Loc Stats_Refresh}"
                           Click="Refresh_Click" />
            </Grid>

            <TextBlock Text="{res:Loc Stats_Hint}"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,24" />

            <!-- Search -->
            <ui:TextBox x:Name="SearchBox"
                        PlaceholderText="{res:Loc Search_Placeholder}"
                        Width="250"
                        HorizontalAlignment="Left"
                        TextChanged="SearchBox_TextChanged"
                        Margin="0,0,0,16" />

            <!-- Rich Empty State: No Telemetry Data -->
            <Border x:Name="EmptyCard"
                    Style="{StaticResource SteamEmptyStateCardStyle}"
                    Visibility="Collapsed"
                    Margin="0,24,0,24">
                <StackPanel HorizontalAlignment="Center">
                    <ui:SymbolIcon Symbol="DataBarHorizontal24" FontSize="40" Foreground="#475C73" HorizontalAlignment="Center" />
                    <TextBlock Text="{res:Loc Stats_NoData}"
                               FontWeight="SemiBold" FontSize="15"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                               HorizontalAlignment="Center" Margin="0,12,0,4" />
                    <TextBlock Text="Play games or trigger a cloud synchronization to view live save metrics and telemetry logs here."
                               FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               HorizontalAlignment="Center" TextAlignment="Center" MaxWidth="440" />
                    <ui:Button Content="{res:Loc Stats_Refresh}"
                               Appearance="Primary"
                               Icon="{ui:SymbolIcon ArrowSync24}"
                               Click="Refresh_Click"
                               HorizontalAlignment="Center"
                               Margin="0,16,0,0" />
                </StackPanel>
            </Border>

            <!-- Rich Empty State: Cloud Storage Not Configured -->
            <Border x:Name="CloudUnavailableCard"
                    Style="{StaticResource SteamEmptyStateCardStyle}"
                    Visibility="Collapsed"
                    Margin="0,24,0,24">
                <StackPanel HorizontalAlignment="Center">
                    <ui:SymbolIcon Symbol="CloudOff24" FontSize="40" Foreground="#E5A93B" HorizontalAlignment="Center" />
                    <TextBlock Text="{res:Loc Stats_NoCloud}"
                               FontWeight="SemiBold" FontSize="15"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                               HorizontalAlignment="Center" Margin="0,12,0,4" />
                    <TextBlock Text="Connect your Google Drive or local storage folder in Cloud Provider to enable telemetry tracking."
                               FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               HorizontalAlignment="Center" TextAlignment="Center" MaxWidth="440" />
                    <ui:Button Content="{res:Loc Nav_CloudProvider}"
                               Appearance="Primary"
                               Icon="{ui:SymbolIcon Cloud24}"
                               Click="GoToCloudProvider_Click"
                               HorizontalAlignment="Center"
                               Margin="0,16,0,0" />
                </StackPanel>
            </Border>

            <ItemsControl x:Name="AppList">
                <ItemsControl.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}"
                                           FontSize="16" FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                           Margin="0,16,0,8" />
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </ItemsControl.GroupStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="4"
                                Padding="12,10"
                                Margin="0,0,0,8">
                            <StackPanel>
                                <!-- Header row -->
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                                        <Button Click="ExpandCollapse_Click"
                                                Tag="{Binding}"
                                                Background="Transparent"
                                                BorderBrush="Transparent"
                                                Padding="4,0"
                                                VerticalAlignment="Center"
                                                Margin="0,0,4,0">
                                            <ui:SymbolIcon Symbol="{Binding ChevronSymbol}"
                                                           FontSize="14"
                                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                        </Button>
                                        <Border Width="92" Height="43"
                                                CornerRadius="4"
                                                Margin="0,0,12,0"
                                                VerticalAlignment="Center"
                                                ClipToBounds="True">
                                            <Border.Background>
                                                <ImageBrush ImageSource="{Binding HeaderUrl, Converter={StaticResource UrlToImageSource}}"
                                                            Stretch="UniformToFill"
                                                            RenderOptions.BitmapScalingMode="HighQuality" />
                                            </Border.Background>
                                        </Border>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding DisplayName}"
                                                   FontSize="15" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                                   TextTrimming="CharacterEllipsis" />
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding AppId}"
                                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                       FontSize="12" />
                                            <TextBlock Text="{Binding Summary}"
                                                       Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                                       FontSize="12" Margin="8,0,0,0"
                                                       TextTrimming="CharacterEllipsis" />
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>

                                <!-- Collapsible details -->
                                <StackPanel Visibility="{Binding DetailsVisibility}"
                                            Margin="36,12,0,0">

                                    <!-- Playtime block -->
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{res:Loc Stats_PlaytimeForever}"
                                                       FontSize="11"
                                                       Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                                            <TextBlock Text="{Binding PlaytimeForeverText}"
                                                       FontSize="14" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{res:Loc Stats_Playtime2Weeks}"
                                                       FontSize="11"
                                                       Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                                            <TextBlock Text="{Binding Playtime2WeeksText}"
                                                       FontSize="14" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="{res:Loc Stats_LastPlayed}"
                                                       FontSize="11"
                                                       Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                                            <TextBlock Text="{Binding LastPlayedText}"
                                                       FontSize="14" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                        </StackPanel>
                                    </Grid>

                                    <!-- Achievements -->
                                    <StackPanel Visibility="{Binding AchievementsVisibility}" Margin="0,0,0,8">
                                        <TextBlock Text="{res:Loc Stats_AchievementsHeader}"
                                                   FontSize="15" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                                   Margin="0,0,0,6" />
                                        <ItemsControl ItemsSource="{Binding Achievements}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid Margin="0,0,0,6">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto" />
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="Auto" />
                                                        </Grid.ColumnDefinitions>
                                                        <ui:SymbolIcon Grid.Column="0"
                                                                       Symbol="{Binding StatusSymbol}"
                                                                       FontSize="18"
                                                                       Foreground="{Binding StatusBrush}"
                                                                       Margin="0,0,10,0"
                                                                       VerticalAlignment="Center" />
                                                        <TextBlock Grid.Column="1"
                                                                   Text="{Binding Label}"
                                                                   FontSize="14"
                                                                   TextTrimming="CharacterEllipsis"
                                                                   VerticalAlignment="Center"
                                                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                                        <TextBlock Grid.Column="2"
                                                                   Text="{Binding UnlockText}"
                                                                   FontSize="13"
                                                                   Margin="12,0,0,0"
                                                                   VerticalAlignment="Center"
                                                                   Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>

                                    <!-- Raw stats -->
                                    <StackPanel Visibility="{Binding StatsVisibility}">
                                        <TextBlock Text="{res:Loc Stats_StatsHeader}"
                                                   FontSize="15" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                                   Margin="0,8,0,6" />
                                        <ItemsControl ItemsSource="{Binding Stats}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                                        <TextBlock Text="{Binding IdText}"
                                                                   FontFamily="Consolas"
                                                                   FontSize="12" Margin="0,0,8,0"
                                                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                                        <TextBlock Text="{Binding ValueText}"
                                                                   FontFamily="Consolas"
                                                                   FontSize="12"
                                                                   Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>

    <!-- Transient status (loading / errors), centered over the view -->
    <TextBlock x:Name="StatusText"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               TextAlignment="Center"
               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
               TextWrapping="Wrap"
               Visibility="Collapsed" />
    </Grid>
</Page>
